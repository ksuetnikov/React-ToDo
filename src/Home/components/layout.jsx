import React from "react";

const Layout = ({ children }) => {
  return (
    // <div className="border-2 rounded-md border-blue-400">
    //   {isCompleted && <BsBookmarkCheck size={64} />}

    // </div>
    // ego ne vstavlyaem
    <div class="check">kokokoko</div>
  );
};

export default Layout;
